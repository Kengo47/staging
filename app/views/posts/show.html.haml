%h1 投稿詳細

.row

  .col-md-6{style: "border: 1px solid gray;"}
    %span title:
    = @post.title
    %br/
    %span body:
    = @post.body
    %br/
    %span picture:
    - if @post.picture?
      = image_tag @post.picture.thumb.url, class: "post"
    %br/
    - if current_user?(@post.user)
      = link_to "delete", post_path(@post), method: :delete, data: { confirm: "You sure?" }, class: "btn btn-danger"
      = link_to "edit", edit_post_path(@post), locals: { post: @post }, class: "btn btn-warning"

  .col-md-6{style: "border: 1px solid pink;"}
    %h5 コメント入力（50文字以内）
    - if user_signed_in?
      = render partial: 'comments/form', locals: { comment: @comment }

    %h5 コメント一覧
    - if @post.comments.any?
      = render partial: 'comments/comments', collection: @comments, as: :comment
    - else
      %p no comments...