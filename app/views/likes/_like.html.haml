- if user_signed_in?
  - if current_user.already_liked?(post)
    = link_to 'お気に入り済', post_likes_path(post.id), method: :delete, remote: true, class: "btn-circle-border-simple2"
  - else
    = link_to 'お気に入り!', post_likes_path(post.id), method: :post, remote: true, class: "btn-circle-border-simple1"