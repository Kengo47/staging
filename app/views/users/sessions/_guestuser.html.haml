= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
  .field
    = f.hidden_field :email, value: "guest@example.com"
    = f.hidden_field :password, value: "123456"
  .form-group
    = f.submit "かんたんログイン", class: "btn btn-primary"